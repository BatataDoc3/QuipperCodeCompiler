# Quipper Code compiler

Simple app to run and see circuits written in the Quipper language.

To run locally you only need to have Docker installed and the provided docker-compose file and run:

```
docker compose -f docker-compose.yml up -d
```
Then open your browser at `http://localhost:300` and that's it!